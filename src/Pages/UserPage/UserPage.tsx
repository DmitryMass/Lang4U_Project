import React, { FC } from 'react';

import styles from './user-page.module.scss';

const UserPage: FC = () => {
  return <section className={styles.userpage}>USERPAGE</section>;
};

export default UserPage;
